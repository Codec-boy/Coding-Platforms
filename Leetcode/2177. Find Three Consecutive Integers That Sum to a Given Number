class Solution {
    public long[] sumOfThree(long num) {
        if (num % 3 != 0)
            return new long[] {};
        return new long[] { num / 3 - 1, num / 3, num / 3 + 1 };



        // long y = num / 3;
        // if ((y - 1) + y + (y + 1) != num)
        //     return new long[0];
        // long[] arr = new long[3];
        // arr[0] = y - 1;
        // arr[1] = y;
        // arr[2] = y + 1;
        // return arr;
    }
}
