class Solution {
    public String replaceDigits(String s) {

        char[] arr = s.toCharArray();
        int n = arr.length;
        for (int i = 1; i < n; i += 2) {
            arr[i] = (char) (arr[i - 1] + (arr[i] - '0'));
        }
        return new String(arr);


        // StringBuilder sp = new StringBuilder();
        // sp.append(s);
        // for (int i = 1; i < s.length(); i += 2) {
        //     sp.setCharAt(i, (char) (s.charAt(i - 1) + (s.charAt(i) - '0')));
        // }
        // return sp.toString();


    //     String str="";
    //     for(int i=0;i<s.length();i++)
    //    {
    //         char ch=s.charAt(i);
    //         if(i%2==1)
    //         {
    //             char c=s.charAt(i-1);
    //             int val=(c-'0')+ch;
    //             str+=(char)(val)+"";
    //         }
    //         else
    //         str += ch+"";
    //    } 
    //     return str;
    }
}
