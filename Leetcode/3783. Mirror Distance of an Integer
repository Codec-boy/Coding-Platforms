class Solution {
    public int mirrorDistance(int n) {

        int rev = 0;
        for (int x = n; x > 0; x /= 10) {
            rev = rev * 10 + x % 10;
        }
        return Math.abs(n - rev);


    //     int rev = reverse(n);
    //     return Math.abs(n - rev);
    // }

    // private int reverse(int n) {
    //     int rev = 0;
    //     while (n > 0) {
    //         rev = rev * 10 + (n % 10);
    //         n /= 10;
    //     }
    //     return rev;
    }
}
