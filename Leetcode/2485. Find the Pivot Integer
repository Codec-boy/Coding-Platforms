class Solution {
    public int pivotInteger(int n) {
        // int i, x;
        // for (x = 1; x <= n; x++) {
        //     int left = 0, right = 0;
        //     for (i = 1; i <= x; i++) {
        //         left += i;
        //     }
        //     for (i = x; i <= n; i++) {
        //         right += i;
        //     }
        //     if (left == right) {
        //         return x;
        //     }
        // }
        // return -1;



        int total = n * (n + 1) / 2;
        int x = (int) Math.sqrt(total);
        if (total == x * x)
            return x;
        else
            return -1;
    }
}
